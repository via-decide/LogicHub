node_modules/
dist/
.DS_Store

# Playwright
test-results/
playwright-report/

# Secrets — never commit. .env.example documents the keys instead.
.env
.env.local
.env.*.local
.env.production

# Local runtime state: _sovereignAuth.js's logRuntimeEvent() fallback log.
data/
