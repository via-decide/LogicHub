# fixtures/kicad/smart-plant-pot/proposed

Proposed revision with controlled changes from the base:

- **J1**: USB connector → battery connector (component replacement)
- **U1**: AMS1117-3.3 LDO → TPS62A02 buck regulator (component replacement)
- **D2**: Schottky diode added (new component)
- **R1, D1**: Status LED circuit removed (component removal)
- **C3**: Additional 100nF cap added (BOM quantity change)
- **J2**: Sensor connector moved to new position (placement change)

These changes exercise the diff engine's ability to detect replacements, additions, removals, quantity changes, and geometry-only moves.

Generated by `../generate-fixtures.mjs` with embedded lib_symbols and deterministic UUIDs.
