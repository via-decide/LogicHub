# @logichub-engineering/git-adapter

Restricted Git operations only: register/validate repo, branches, commit resolution, ancestry, stale-base detection, controlled merges. No arbitrary shell strings from user input.

No implementation yet — Phase 3.
