# @logichub-engineering/kicad-adapter

Parses .kicad_pro/.kicad_sch/.kicad_pcb, extracts BOM and semantic objects, renders schematic/PCB, runs ERC and DRC inside an isolated, version-pinned KiCad environment.

No implementation yet — Phase 4.
